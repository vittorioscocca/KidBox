One-line: The circular orange-gradient "parla con l'AI" FAB — sparkles glyph, white ring, glow and a breathing pulse.

```jsx
<AskAIButton onClick={openAI} />          {/* 58px floating */}
<AskAIButton size="sm" pulse={false} />   {/* 42px inline   */}
```

Place `fab` bottom-right with ~20–32px inset (the real Home placement). Set `pulse={false}` for static contexts.
