One-line: The "Invita l'altro genitore" prompt card — icon, copy, trailing chevron on a warm surface.

```jsx
<InviteCard onClick={openInvite} />
<InviteCard title="Aggiungi un familiare" subtitle="Condividi il codice" onClick={...} />
```

Defaults to the parent-invite copy. Full-width; drop it under the Home grid when the family has fewer than 2 members.
