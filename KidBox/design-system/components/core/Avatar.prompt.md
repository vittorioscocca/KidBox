One-line: Circular avatar with a hairline ring; shows a photo, else initials, else a person glyph.

```jsx
<Avatar name="Vittorio Scocca" />
<Avatar src={photoUrl} name="Anna" size={48} />
```

`size` is the diameter in px (default 40). `name` drives the initials fallback and alt text.
